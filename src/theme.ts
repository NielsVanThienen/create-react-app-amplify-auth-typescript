import { Theme } from "@aws-amplify/ui-react";

const theme: Theme = {
  name: "cra-my-theme",
  tokens: {
    colors: {
      font: {
        primary: { value: "#E13300" },
      },
    },
  },
};

export default theme;
